{
  "name": "deadbox-example",
  "version": "0.0.0",
  "description": "Shows how to plug in deadox to a Hapi server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "BSD",
  "dependencies": {
    "hapi": "4.1.x",
    "deadbox": "0.0.x"
  }
}
